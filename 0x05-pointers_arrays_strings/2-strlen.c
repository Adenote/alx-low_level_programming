#include "main.h"

/**
 *_strlen -  function that returns the length of a string
 * @s: this is entry
 * Return: lenght of the string
 * index: this is an input
 *
 */

int _strlen(char *s)
{
	int index;

	for 
		(index = 0; s[index] != '\0'; index++);
	return (index);
}
