#include "main.h"

/**
 *_isalpha - check for alphabet character
 *@c: is an integer
 *Return:0 or 1
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && C <== 'Z'))
		return (1);
	else
		return (0);
